# Core pipeline — プロバイダはモデル名で切り替わる（pipeline/llm.py）
google-genai>=2.8.0     # Gemini 経路
openai>=1.109           # OpenAI 経路（gpt-transcribe ほか）
python-dotenv>=1.0      # .env loading (pipeline/config.py)
requests>=2.32          # HeyGen API (pipeline/heygen.py)

# Document input — tools/gen_notebooklm_prompt.py
python-docx>=1.2        # .docx reader
pdfplumber>=0.11        # .pdf reader

# Tests
pytest>=8.0            # 自動テスト（tests/ 配下、APIは呼ばない）

# Frame analysis — tools/investigate_caption.py
pillow>=11.0
numpy>=2.0
