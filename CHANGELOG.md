# 変更記録

判断の理由を残す場所。**手で書きます**（`docs/` と `CLAUDE.md` は
`tools/gen_docs.py` / `tools/gen_spec.py` の生成物なので、手で書いても再生成で消えます）。

コードを読めば分かることは書きません。書くのは「なぜそうしたか」だけです。

---

## 2026-08-05 — 台本の冗長さ対策・review 工程の新設・入口の一本化

### 発端

出来上がった英語台本が回りくどい、という指摘。挙がった具体的な症状は5つ。

1. **導入部の引き伸ばし** — 本題に入るまでに情景描写（「肥沃な土の匂い」等）が数段落続く
2. **同じ主張の繰り返し確認** — 「この物理的な不一致は〜を示しています」→「この動態は〜を意味します」→「この現実は〜を裏付けます」
3. **メタファーの多用と説明の二重化** — 比喩を出す→比喩自体の仕組みを説明→元の話に戻す、の往復
4. **結論の予告と回収の反復** — 節末で「〜という重大な意味を持つ」と締め、次節でまた同じ含意を掘る
5. **総括での再要約** — 最後に全体をもう一度なぞる（要約の中の要約）

論理展開は一本筋が通っているが、各ステップで「これは何を意味するか」を
過剰に説明し尽くしてから次へ進むため、情報密度に対して文章量が多い。

### 原因の切り分け

三つあり、いずれも別々に手当てが要った。

**(a) モデルが安すぎた。** `REWRITE_MODEL` は `gemini-3.5-flash`（最安ティア）だった。
さらに `rewrite.py` は以前から `effort="high"` を渡しているが、**これは OpenAI 経路でしか
効かない**ため、最難関の工程で深い推論が一度も使われていなかった。

**(b) プロンプトが水増しを指示していた。** 旧「Coverage — CRITICAL」節は
「要約するな・全網羅せよ・**同等の長さに**・迷ったら**足せ**」と書いていた。
長さを目的にすれば、モデルは言い換えで字数を稼ぐ。

**(c) 素材が二人対談である。** これが最大の要因。話者Aの主張を話者Bが
言い換えて同意する構造が、そのまま症状2の「言い換えの連鎖」になる。
対談の冗長さは内容の冗長さではなく**形式に由来する**ので、
「短く書け」ではなく「同意のやり取りは1論点として一度だけ書け」と言う必要がある。

### 1. モデルを上げた（`pipeline/config.py`）

| 工程 | 変更前 | 変更後 |
|---|---|---|
| rewrite | `gemini-3.5-flash` | `gpt-5.6-luna` |
| review（新設） | — | `gpt-5.6-luna` |
| translate | `gemini-3.6-flash` | 据え置き |

`PRESETS["gemini"]` の rewrite も 3.5 → 3.6 に。`.env` の雛形も更新。

### 2. rewrite のプロンプトを全面刷新（`pipeline/rewrite.py`）

優先順位を明示した構成にした（競合したら上が勝つ）。

```
0 主題の一致 > 1 絶対厳守 > 2 忠実性 > 3 対談の一本化 > 4 構造 > 5 明快さ > 6 音声 > 7 引き込み
```

症状への対応:

- §1.1 一論点一記述（症状2）／§1.2 情景描写の導入禁止（症状1）
- §1.3 比喩は一度だけ、往復して再説明しない（症状3）
- §1.4 総括の全面禁止（症状4・5）／§1.5 含意は事実1つにつき1文まで
- §1.6 文長を変える（同じ調子の断定が続くのを防ぐ）／§1.7 定型のつなぎを削る
- §2.2 **目標を元の 60〜75%**（旧「同等の長さに」を置き換え）
- §3 対談の一本化（原因(c)への対応。最も効くはずの節）
- §5.1 専門用語は初出時に一節だけ注釈、以後は素で使う

**ポッドキャスト放送前提の §6 を追加。** 視覚参照の禁止、帰属は文頭
（"According to …"）、数字は耳で追える表記で1文に2個まで、一文一義。

意図的に採用しなかったもの:

- **`[SECTION: ...]` ヘッダーと目次** — 出力は全文が HeyGen で読み上げられるため、
  見出しがそのまま音声になる。話題の境界は `<break time="1.0s"/>` で表す。
- **台本全体を通した「開いた輪」の伏線** — transcript を 7,000字ごとに分けて
  生成するため、中間の塊は伏線の回収を保証できない。フックは最初の塊、
  締めの問いかけは最後の塊にだけ割り当てた（`_POSITION`）。

検証の整合: `_MIN_RATIO = 0.45` は据え置き。新しい目標 60〜75% はその上にあり、
下限は「明らかな要約」を弾くための保険として機能し続ける。

### 3. review 工程を新設（`pipeline/review.py`）

台本が報道として成立しているかを、**元の文字起こしと突き合わせて**点検する工程。
読み返しの効かない音声では、裏付けのない一文もそのまま事実として受け取られる。

```
transcript/ → [rewrite] → draft/parts/ → [review] → narration/parts/ → 以降は従来通り
```

ステージ `draft` を挿入し、rewrite の出力先を `narration/` から `draft/` に移した。
以降の工程（concat_narration / translate / heygen）は `narration/` を読むままで変えていない。

**判定軸** — high: 文字起こしに無い事実・数字／hedging の消失／予測の断定化／
当事者の主張が地の文になっている／帰属なき告発／数値の食い違い。
medium: 煽り語・論説の混入・偏った語り・反論の脱落・未注釈の専門用語・重複。
low: 無意味な強調語。

**修正方針** — 削除ではなく帰属・限定の付与を優先する。文字起こしに存在しない
主張のときだけ削る。新しい事実の追加と字数の増加は禁止。

**検証** — draft の 75〜115% に収まらなければ作り直す。下限は「校閲のつもりで
要約した」を、上限は「直すついでに書き足した」を弾く。**上限がないと、
rewrite で削った冗長さがこの工程で戻ってくる。**

**指摘が0件なら台本には触らず複製する。** 理由の説明がつかない書き換えを防ぐため。

**記録** — `draft/parts/{stem}_part01_review.md`。録音前にここだけ読めばよい。

**`_drop_orphans()`** — 台本を作り直して part 数が変わったとき（7分割→5分割）、
`narration/parts/` に残った古い part06・part07 が次の concat で本文に混ざる。
出来上がりを読むまで気づけないので、draft に対応のない出力と記録を消す。

`--model-review` / `--steps review` / `step_review.sh` も追加。テスト12件。

### 4. 入口を `run.sh` に一本化

`run_pipeline.py` を呼ぶ入口が3本あり、包含関係で重複していた。

- `run.sh` = `run_pipeline.py "$@"` そのまま
- `run_audio.sh` = 引数なしで実行すると run.sh と同じ（＋音声指定＋inbox 片付け＋成果物表示）
- `run_again.sh` = run.sh --force ＋ 対象の自動選択

`run_audio.sh` と `run_again.sh` を削除し、機能を `run.sh` に統合した。

```bash
./run.sh                    # inbox の全件
./run.sh audio.m4a          # 音声1本を指定
./run.sh --again            # 既存プロジェクトを上書きして作り直す
./run.sh --no-video         # 動画を作らず台本と日本語訳まで
./run.sh --keep-inbox       # 処理後も inbox に残す
```

`--no-video` は `.sh` ではなく `run_pipeline.py` の引数層に置いた。3本の入口が
いずれも引数を透過するので、1箇所の実装で全部に効く。工程名はベタ書きせず
`ALL_STEPS` から `_VIDEO_STEPS` を除く形にして、工程が増えても追従するようにした。
`--steps` との併用は、どちらを尊重すべきか決められないのでエラーにする
（黙って片方を無視しない）。

`run_no_video.sh` は `./run.sh --no-video` への1行の近道。`--no-video` を
**引数の後ろ**に置いているのは、run.sh が音声パスを第1引数として扱うため。

### 5. 積み残しの是正

`3f19ce5`（8/1、動画生成の復活）で `ALL_STEPS` に heygen と concat_video が
戻されたが、**4ファイルのコメントが「保留中のため実行されません」のままだった**
（`run.sh` / `run_audio.sh` / `step_heygen.sh` / `step_concat_video.sh`）。
実挙動と食い違っていたので、すべて実態に合わせた。

### 作業中に踏んだ落とし穴

**`docs/` と `CLAUDE.md` は生成物。** `tools/gen_docs.py` の冒頭に
「Never edit files in docs/ by hand」と書いてあるとおりで、手で入れた
review 工程の説明と `REVIEW_MODEL` の行は、次のフック実行で消えていた。
生成側（`gen_docs.py` / `gen_spec.py`）に入れ直した。**この記録を `docs/` に
置かないのも同じ理由。**

**入口の統合で回帰を1つ作った。** `run_audio.sh` の「inbox が空なら止まる」を
そのまま持ち込んだため、`./run.sh --steps rewrite`（既存プロジェクトの工程だけ
やり直す）が inbox 空を理由に失敗した。音声もプロジェクトも無い場合は
`run_pipeline.py` が案内するので、run.sh では止めない形に直した。

**作業中に `data/` の中身が外部から消えた**（8/5 14:24:48）。プロジェクト、
`inbox/`、`senario_jp/` がすべて空になった。`tools/clean_data.py` の実行と
思われる。`data/` は git 管理外のため復元できない。新構成へ無停止で移行するため
`narration/parts` を `draft/parts` へ複製していたが、これも失われた。

### 次にやるとき

新しいプロンプトで作り直す:

```bash
./run.sh --again --steps rewrite,review    # あるいは ./run_no_video.sh --again
```

効果の確認は rewrite のログに出る「合計◯字 = transcript の◯%」で見る。
**旧プロンプトでの実測は 89%、新しい目標は 60〜75%。**
報道面の確認は `draft/parts/*_review.md` の high 指摘から読む。

---

## 2026-08-05（同日追記）— 初回実走で見つかった2つの癖への調整

49分の実音声（52,416字）で初めて通し実行した結果。**成果は狙いどおり**
（rewrite 66%・旧プロンプト実測89%から改善、review が high 81件を検出して修正、
完走 27分）。ただし走らせて初めて見える癖が2つあった。

**(1) rewrite の初回出力が短すぎる。** 8チャンク中、初回で通ったのは4つ。
2つは 22%・18% まで圧縮して棄却された（残り2つは `<speak>` 複数と途中切れ）。
「TIGHT に書け」が長さ目標より強く効いていた。対処:

- 字数目標（60〜75%）をプロンプト冒頭の「TWO NUMBERS GOVERN EVERYTHING」に昇格。
  **床でもある**こと（「情報を落とした簡潔さは失敗」）を明記
- 検証エラーを次の試行に渡す `_RETRY_BLOCK` を rewrite / review 両方に追加。
  それまでは同じプロンプトを投げ直すだけで、同じ失敗を繰り返していた
- `<speak>` 複数は §4「ブロックに分けろ」の誤読。「ブロック＝segment ではない、
  全体で `<speak>` 1つ、区切りは break のみ」を §4.4 と §9 に明記

前回初回18%だった chunk 7 で単体検証 → 初回一発 77%・`<speak>` 1個。

**(2) UNSUPPORTED が 63件。** rewrite の「分析を足せ」(§2.5) が事実の追加に
滑っていた（比較級 "faster" の付加、"war chests" → "military reserves" への
言い換え等）。review が毎回削るので成果物は守られるが、往復が無駄。対処:

- §2.6 ANALYSIS ADDS NO FACTS — 解釈は transcript 内の事実を繋ぐもの。
  数字・日付・主体・比較級・帰結の追加は分析ではなく捏造と明記
- §2.7 QUOTE THE SOURCE'S OWN TERMS — 係争的・精密な語は原語のまま。
  流れのための言い換えは自由、実質のための言い換えは禁止

review はこの調整後も安全網として残る。狙いは検出件数を減らすことではなく、
**rewrite が最初から正しく書き、review の修正が例外になる**こと。

---

## 2026-08-05（同日追記2）— 完成台本のレビューを受けた恒久対応

生成された台本を通しで読んだレビューへの対応。指摘は6系統で、
台本を個別に直すのではなく、すべてパイプライン側に恒久実装した。

**(1) SSML の破綻（配信事故リスク）** — `<break time="0.5s/>`（閉じ引用符欠落）が
最終台本まで残っていた。壊れたタグは「正しい break を数える」検証には一致しない
ため素通りしていた。`ssml.check_tags()` を新設し、`<speak>` と正規の `<break>` 以外の
タグ様のものを白リストで弾く。rewrite / review 両方の検証に組み込み。
HeyGen が解釈しないタグは読み上げられてしまうので、`<emphasis>` の追加は見送った
（対応が実証できたら再検討。`tool_heygen_test.sh` で試せる）。

**(2) 数字の表記揺れ** — "two thousand eight" と "176,000" が混在。TTS の誤読を
防ぐため §6.4 を全面スペルアウトに変更（型式番号 PAC-3 / Boeing 747 だけ例外）。
review にも DIGITS / INCONSISTENT NAME（Balti/Balzi Rossi の揺れが実例）の
判定を追加。

**(3) 構造が耳に見えない** — §4.5 を章のサインポストに変更。章境界で
「どこまで来て、次はどこへ行くか」を1〜2文で口頭に出す。

**(4) 冒頭の伏線が放置される** — フックは冒頭でチラ見せ→回収は終盤80%地点なのに、
中盤に引き戻しが無かった。塊分割生成では後続の塊が冒頭を知らないのが原因。
part01 からフックを抽出して後続塊に渡す `_HOOK_BLOCK` を実装。章を閉じる塊は
1文だけコールバックでき、最終塊は明示的に回収する。

**(5) ホストの不在** — §7.5（章ごとに1〜2回の一人称の反応）と §7.6（分析での
"we"）を追加。事実の記述とホストの主観は区別し、ヘッジは維持。
`.env` の NARRATION_OPENING を番組紹介入りに差し替え（**文言は要確認**）。

**(6) 配信前ファクトチェック** — `factcheck` 工程を新設。review が「台本 vs
文字起こし」なのに対し、factcheck は「素材 vs 現実」で照合すべき項目
（人物と役職・数値・日付・固有名詞）をリスク順の表に書き出す。検証はせず、
録音前の人間のチェックリストにする。モデルは REVIEW_MODEL に従う。

**ドメイン汎用性** — 上記の新プロンプトはすべて例示を複数分野（金融・スポーツ・
科学・軍事）に分散させた。NotebookLM の `_META_PROMPT` には報道規律の節
（日付の絶対化・主張の分類とヘッジ・利害当事者の帰属・数字と氏名の明示発話・
陣営に依らない中立トーン）を必須要件として追加した。review は「台本 vs
文字起こし」しか照合できないため、音声の元になる NotebookLM プロンプト自体が
規律を持たないと下流では検出できない。

**実走で判明した3つの取りこぼし**（同日、上記の直後に修正）

新プロンプトで通した結果、狙いの半分しか効いていなかった。

- **rewrite と review が矛盾していた。** review の EDITORIALISING 判定が、
  rewrite に書かせたホストの一人称（"The timing is the detail I would not dismiss."
  "When I follow that chain one step further..."）を削っていた。**片方の工程に
  書かせたものを、もう片方が消す**という設計ミス。review 側に例外を明記:
  人物への道徳的断罪と、意見を事実として述べるものは指摘対象。証拠に対する
  ホストの主観は削らない（このナレーションには名前を出す語り手がいる）。
- **サインポストと引き戻しが上位規則に負けていた。** §1.4（要約禁止）と
  §1.7（つなぎ削除）が、§4.5 のサインポストと §7 のコールバックを打ち消していた。
  優先順位の上位規則が下位の新機能を殺すのは当然で、両規則に明示の除外を追加。
  §7.5 も「任意」ではなく REQUIRED と明記（ENGAGEMENT は最下位優先度のため）。
- **表記揺れは part をまたぐと原理的に見つからない。** 同一のレストランが
  part05 で "Balzi Rossi"、part07 で "Balti Rossi"。review は1 part ずつ見るため
  単独では検出不能だった。済んだ part から固有名詞を集めて次の part に渡す
  `known_names()` を実装。実測: part07 の再点検で "Balti Rossi" を
  INCONSISTENT NAME として検出し、修正できた。

数字のスペルアウトとタグ検証は初回から機能した。最終台本に残った数字は
Mi-8 / Euro 5 / S-300 など公式の型式表記のみで、これは意図した例外。
生成中に `</break>` という不正タグが1度出たが、検証が弾いて作り直しで解消した
（従来なら最終台本まで残っていた）。

**目標字数を 60〜75% → 65〜80% に引き上げ**（同日）。修正後のプロンプトでの
実測が draft 81% / 最終 76% となり、旧目標を超えた。増分 +3,639字は今回入れた
一人称24文・章のサインポスト・密な説明を割る問いとほぼ一致する。

元の指摘は「**同じ主張の繰り返し**で離脱する」であって「長い」ではない。
サインポストとホストの声は離脱を**防ぐ**側の要素なので、これを削って
目標に収めるのは本末転倒になる。旧プロンプトの 89% からは十分下がっている。
プロンプトには「この幅は聞き手の道案内のためであって、既出の主張を
言い直すためではない」と明記した。

---

## 2026-08-05（同日追記3）— 2回目のレビュー: 人格注入の副作用と帰属の穴

改善は確認された（サインポスト、ホストの人格、タグ修正、表記統一、
フルサークルの締め）。一方、**修正の過程で新しく生まれた癖が3つ**あった。
いずれも「軍事だから」ではなく、どのテーマでも再発する型なので恒久ルール化した。

**(1) 定型句の口癖化。** "what caught my attention" 系が40分で6回。人格注入が
テンプレ化し、**AIらしさを復活させていた**。§7.5a で「同じ反応の型は全体で2回まで、
言い回しの違いは同一の型と数える」を明記。review に CATCHPHRASE 判定を追加。

**(2) インライン用語解説の過剰。** ダッシュ挟みの解説が8箇所に増え、
"a logistics hub — a site that stores and moves supplies" のような**日常語まで
解説していた**。聞き手を子ども扱いし、削ったはずのテンポを食う。§5.1 に
仕分け基準（通常の報道で説明なしに使われる語は解説しない）。review に
OVER-EXPLANATION 判定。

**(3) "our sources" — 最も危険。** 番組が独自の情報源を持つように聞こえるが、
実際は公開情報を読んでいるだけ。**原因は文字起こし側**で、NotebookLM の話者が
3回そう言っており、rewrite が忠実に運んでいた。3箇所で断つ:
NotebookLM の meta prompt（上流で言わせない）、rewrite §6.3a（運ばず書き換える）、
review の FALSE SOURCING（HIGH 判定で検出）。

**未対応だった中盤の re-hook** — `_HOOK_BLOCK` が "MAY" だったため一度も
出なかった。章を閉じる塊では「必須」に格上げ。

**トーンの深刻度判定**（§8.1）を追加。死傷者・災害・戦争を扱う箇所で軽口や
エンタメ的な煽りを禁じ、経済・技術・スポーツでは驚きやユーモアを許容する。
**あらゆるニュースを扱うなら、災害報道と経済解説で同じテンションは使えない。**

**細目** — 型番の読み方をスペルアウトで固定（"S-400" は合成音声が
"S マイナス400" と読みうる）。時制の揺れ（§8.2）。編集の名残と思われる
浮いた一文の禁止（§1.10）。連続 break は**プロンプトではなくコードで**統合
（`ssml.merge_breaks()`）— 決定論的に潰せるものをモデルに任せる理由がない。

**見送り: `<emphasis>` の付与。** HeyGen が解釈する保証が無く、解釈されない
タグはそのまま読み上げられる。`./tool_heygen_test.sh` で実機確認が取れたら入れる。

**Chaiko 将軍の役職**（"He serves as the commander of the Russian Aerospace
Forces"）は断定のまま残っている。これは台本側で直す種類のものではなく、
factcheck が拾って**人が一次ソースで照合する**項目。factcheck に
「人名と役職の組み合わせは例外なく High」を明記し、docs に
「High の行は照合完了まで配信しない」運用を書いた。

**プロンプトの肥大を検査してから縮めた**（同日）。指摘を足し続けた結果、
指示16,230字に対し素材7,000字（**2.73:1**）、番号付き規則48件になっていた。
実装前にモデル自身へ監査させたところ、**実際に動作を変える矛盾が11件**見つかった。

主なもの:
- §6.4「数字を書くな」と §9「`<break time="0.5s"/>` を入れよ」— **適用範囲を
  書いていなかった**ため字義どおりには両立しない
- §10「hook から始めよ」と中間セクションの「hook を書くな」
- §2.1「全ての名前を残せ」と §3.1「話者に言及するな」— 話者名の除外が未記載
- §6.5「氏名と役職を full で言え」と §2.6「事実を足すな」— 素材に役職が無い場合
- §4.5「次章を名指ししてサインポストせよ」— **中間の塊は前後を見られない**ので不可能
- §7.5/7.5a の「全体で2回まで」— 塊ごとに生成するため**全体の回数を知りようがない**

パッチではなく全面的に書き直した。重複していた規則を統合し
（1.1/3.2/3.5/1.4 の反復禁止を1条に、2.1/2.2 を冒頭の宣言に吸収、
7.5/7.5a を1条に）、優先順位に抜けていた TONE・SSML・出力形式を明記した。

**13,544字・35規則（指示:素材 = 2.22:1）** に縮小。同じ監査を再度かけて
11件の矛盾が消えたことを確認し、残った境界条件6件も潰した
（重複削除の結果として下限を割る場合、確信度の違う言い直しの扱い、
素材が stakes を語っていない場合、章の定義、疑問文の頻度の上下限衝突、
"our sources" を消すと不確実性まで消える件）。

---

## 2026-08-05（同日追記4）— パイプラインの再設計: 生成で禁じるのをやめ、検出して直す

Chaiko 将軍の役職は**正確**だった（2026年5月に航空宇宙軍司令官へ就任、
国防省が6月10日に公式発表、EU の Bucha 関連制裁とも整合）。この項目はクローズ。

3回目のレビューで**同じ型の失敗が3度目**だと分かった。
"caught my attention" を禁じたら "When I first saw that figure" が5回になり、
中身も「数字に驚く」という同一の芸を3回繰り返していた。
**「Xを使うな」と指示すると、モデルは別の単一テンプレートへ収束する。**
そして re-hook は2回続けて未実装だった。

原因は指示の弱さではなく設計にある。

**(1) 生成時に禁じようとしていた。** モデルは書きながら自分の口癖を数えられない。
一方、**完成文から数えるのは確実にできる**。生成と検出では信頼性が段違いである。

**(2) 点検を塊ごとに行っていた。** 定型句の総回数も、冒頭の伏線への引き戻しの
有無も、締めが冒頭に回帰しているかも、**全文を見なければ判定できない**。
re-hook が実装されなかったのは、点検する側に見えていなかったからである。

**(3) 検出と修正が1回の呼び出しに同居し、全文を書き直させていた。**
修正対象でない文が巻き添えで壊れた（"two-thirds of demand" の脱落、
ホストの一人称の削除）。

### 作り直した形

```
transcript → rewrite → draft/parts → assemble → draft/_full
           → polish（A検出→Bパッチ→C検証、最大2周）→ narration/_full
           → factcheck / split / translate
```

- **A 検出**（`polish.detect`）— 書き換え禁止、欠陥リストのみ。
  反復フレーズ・壊れたタグ・数字・表記揺れは `pipeline/defects.py` が
  **コードで数える**（モデルに数えさせない）。モデルが担うのは修辞の反復・
  文意の破綻・必須要素の有無・帰属とヘッジ・根拠照合だけ。
- **B パッチ**（`polish.repair`）— `old→new` の組だけを出させ、
  **文字列の置換はコードが行う**。原文に厳密一致しない引用と、一意に決まらない
  引用は棄却する。**巻き添え破壊が構造的に起きない。**
- **C 検証** — 数値と固有名詞の消失は**機械照合**で先に弾き、残りをモデルが見る。
- 最大2周。残りは `narration/{stem}_defects.md` に「既知の残存欠陥」として記録。
  ゼロを追うと収束しない。

`review` 工程は廃止（役割は polish に吸収）。`concat_narration` は `assemble` に
改称して**点検の前**へ移動し、HeyGen 向けの分割は点検の**後**の `split` に分けた。
定型の挨拶とアウトロは全文に1回だけ現れるものなので、塊ごとの生成ではなく
`assemble` が入れる。

**rewrite プロンプトからは検出側が拾う規則を削除**した（反復禁止・口癖禁止・
浮いた一文・連続 break）。32規則に減り、冒頭に「後段が点検するので
防御的に書くな」と明記した。

実データでの確認: 現行の最終台本に対して検出器を回すと
**"when i first saw" 5回**を最上位で捕捉した（レビューの指摘と一致）。

**新しいプロンプトの汎用性を監査した**（同日）。polish の検出プロンプトと
rewrite の例示が軍事語に偏っていた（refining / air-defense / general /
crater / warhead）。スポーツ・経済・科学の transcript を、戦争の語彙で
書かれた点検表にかけることになる。

例をすべて分野横断に置き換えた。検出プロンプトの冒頭に
「話題は何であれ、以下の観点はそのまま適用される。例示ではなく
台本自身の主題に照らして判定せよ」を明記。
文意の破綻は4類型（短縮で意味が消えた／接続詞が繋がらない／数と述部の
不一致／前の稿の名残）に整理し、いずれも分野に依存しない形にした。

rewrite に残した軍事語3件は、**汎用性を守る規則そのもの**である
（分野の押し付け禁止の例、語の格上げ禁止の例、深刻度判定）。

**新しいプロンプトも監査してから使う**（同日）。前回の教訓どおり、書いた直後に
モデルへ監査させた。A検出とBパッチで実害のある欠陥が見つかった。

検出パス:
- **台本と文字起こしの区別がプロンプトに書かれていなかった**。区切りマーカーは
  コード側で付けているが、プロンプトが説明していないので、どちらを検査するのか
  判断できない状態だった
- 「合格も yes/no で答えよ」と「欠陥だけ返せ」が両立しない。スキーマに
  「合格」を表す形が無く、`quote` は必須なのに欠落項目には引用元が無い
- 「反復は全出現箇所を引用せよ」が、`quote` 1つのスキーマと矛盾
- 章の定義が無い / 冒頭の問いが無いとき、それに依存する検査が宙に浮く
- 同一文が複数あると一意な引用が作れない

パッチパス:
- 「出力は old と new の組だけ」と「why も必須」が矛盾
- 規則1「リスト外に触るな」と規則6「全文の SSML と数字表記を守れ」が矛盾
- **`<break time="1.0s"/>` の秒数は数字なのに「数字はすべて綴れ」と衝突**
- 冒頭への挿入が表現できない
- `old` は原文と適用後のどちらに一致するのか未定義 → **重なる修正は
  適用順で結果が変わる**

最後の1件はプロンプトだけでなく **コードでも塞いだ**（`apply_patches` が
原文上の位置で重なりを検出して棄却する）。位置を適用後の本文で測ると、
先の置換で長さが変わったぶんだけ範囲がずれて重なりを見落とす。

再監査で構造的な矛盾は消え、残った境界条件6件も潰した。

**新設計の初回実走とその結果**（同日）。

```
rewrite 8チャンク（初回成功 7/8、77%）→ assemble 40,679字
polish 第1周: 欠陥57件 → 適用38 / 棄却11
       第2周: 欠陥29件 → 適用12 / 棄却9
```

機械的な欠陥（定型句・不正タグ・連続 break・桁数字・`our sources`）は**すべて0**。

**棄却の内訳がこの設計の価値だった。** C検証が意味の劣化を12件、機械照合が
固有名詞の消失を4件、`apply_patches` が巻き添え破壊を4件（引用不一致2・
範囲の重なり2）止めた。前の設計なら全文書き直しで素通りしていたものである。

### 実走で見つかった3つのバグ

**(1) C検証が A の意図した修正を潰していた（自家中毒）。** A が
「断定を可能性に直せ」と指摘 → B が直す → C が「事実が仮定に弱められている」で
棄却、という循環が12件。C は差分しか見ないので、**弱めるのが目的の修正と
うっかり弱まった修正を区別できない**。修正の理由を C に渡し、
「理由が求めた変化なら正しい」と明記した。
rewrite と review が矛盾していた件と同じ型の失敗である。

**(2) 記録の「残存欠陥」が最後の修正より前の検出だった。** ループが
検出→修正で終わるため、最終修正で直した分まで残存として並んでいた。
**検出で始まり検出で終わる**形に変えた（API 呼び出しが1回増える）。

**(3) 表記揺れが同数のとき、寄せ先を頻度で断定していた。**
実際 `Balti Rossi` 1回 : `Balzi Rossi` 1回 で、**文字起こし自体が割れていた**。
根拠照合では決着しないので、台本内で揃えたうえで factcheck の綴り確認に委ねる。

### レビューが見つけた新しいテンプレ収束

**re-hook 指示が効きすぎ、"the answer to the central question" 型が6回**出現した。
エッセイが自分の目次を読み上げている状態で、**これは私が入れた修正が生んだ
新しい口癖**である。同じ現象が3度目（caught my attention → When I first saw →
the central question）。

対策は禁止ではなく検出。`META-NARRATION` を検出カテゴリに追加した:
- 音声で追えない自己参照（"the area described at the opening"）
- 目次ナレーション（"the answer to the opening question continues"）
- **構造への言及は全体で2回まで**。前方参照は「問い」ではなく
  **待たせている物そのもの**（あのレストラン、あの火災）を名指しする

あわせて、人称を消して残った幽霊文（"I assumed I had misread it" →
"That range initially appeared easy to misread"）を防ぐため、
パッチ側に「主観の除去は文ごと削除であって、非人称化ではない」を明記し、
C 検証の「単独で読めるか」を「単独で**意味を持つか**」に強めた。

**固定の語彙表をやめ、台本ごとに導出させる**（同日）。

口癖の対策として一度「仕掛けの一覧表」（ホストの反応・比喩・修辞疑問…に
それぞれ上限を持たせた表）を作ったが、**これも症状の列挙だった**。
ニュースの主題によって使う仕掛けは違う（市場の解説とスポーツの試合記事と
感染症の調査で、同じ表は当てはまらない）。表を作った時点で局所最適である。

削除し、検出側に「**この台本から仕掛けを自分で見つけて数えよ**」と要求する形にした。

    与える一覧は無い。台本を読み、語り手が同じ仕事のために繰り返している
    構文を見つけ、自分の言葉で名前を付けて数える。3回以上使われている
    ものを報告する。とくに、台本が入れるよう求められた要素をよく見ること。
    **要求された要素こそが型に落ちる。**

生成側も同じで、個別の上限ではなく原則1つ（同じ仕掛けの2回目以降は
互いに似ていてはならない）に統合した。

あわせて重複を削除:
- 反復禁止が §1 に4条あったのを1条に（すでに実施済み）
- §2.3「分析は事実を足さない」と §2.4「素材の語を変えない」を統合
- §4.2 の章サインポストの説明が §7.2・§7.6 と重なっていた分を削除
- §6.2 と §6.7（耳で追える文）を統合
- §1.2「前置きを書くな」と `_POSITION["first"]` の重複を解消

最終監査で出た8件（長さの単位が語数と文字数で二重、話者名の扱い、
章境界の間が1.0秒と1.5秒の両方に該当、修辞疑問の頻度の上下限衝突、
素材が略称しか与えていない場合、比喩と「比較を足すな」の衝突、
根拠の無い対立）も一行ずつ潰した。

**32規則・13,285字**。検出6,438字、パッチ4,004字。

**全修正込みの初回実走**（同日）。レビュー指摘4件のうち3件が解消した。

| 指摘 | 前回 | 今回 |
|---|---|---|
| 幽霊文（人称を消した残骸） | 1件 | **0** |
| 自己参照（"the area described at the opening"） | 1件 | **0** |
| メタ参照（"the central question" 型） | 6回 | **2回** |
| 綴りの揺れ（Balti / Balzi） | 1件 | 1件（下記） |

polish は 32件 → 適用17、18件 → 適用2、残存14件。前回（57→38、29→12、残存29）
より検出も残存も減っている。

### 口癖検出の一般化が効いた

固定表を捨てた結果、**モデルが自分で見つけた仕掛け**が報告に出た:

    [HABIT] 「一段先まで追う」という因果連鎖への導入を脚本全体で4回使っており…

これは §7.3 に書いた "if we follow the money one step further" が
テンプレート化したもので、**要求した要素が口癖になる現象の4例目**である。
今回は一覧に書いていないのに検出できた。列挙をやめた効果が実測で出ている。

### 機械照合の誤検出が2つ見つかった

どちらも「消えた」と「直した」を区別できていなかった。

**(1) 綴りの統一を欠落として棄却していた。** `Balti Rossi` → `Balzi Rossi` の
修正が2周とも「固有名詞が消えた」で弾かれ、**表記揺れが最後まで直らなかった**。
new にほぼ同じ綴り（編集距離2以内）があれば統一とみなすようにした。

**(2) 慣用句の "one" を数として数えていた。** "follow the money one step further"
の言い換えが「one が消えた」で棄却された。単独の "one" だけ除外する
（two 以上は "three people were injured" のように実際の数であることが多い）。

C検証の自家中毒（意図した弱化を棄却する）は前回12件だったが、理由を渡す
修正で今回は0件になった。棄却13件の内訳は上記の誤検出と、引用不一致・
範囲の重なりである。
